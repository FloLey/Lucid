# Required for Backend
GOOGLE_API_KEY=

# Optional: defaults to http://backend:8000 in Docker
VITE_API_TARGET=

# Set to 1 to enable JSONL debug logging of all LLM calls (backend/logs/llm_debug.jsonl)
LLM_DEBUG_LOG=
